// how to varible dicleare
const fathersName = 'Abbajan'
let seson = 'shith khal'
seson = 'gorom khal'

// condition dicleare > < === !== <= >= 
// multiple contdition && ||

if (fathersName == 'abbajan' || seson === 'shith khal') {

}
else if (fathersName === 'Abbajan') {

}
else {

}

// array dicleare
// indes length push 
const number = [34, 23, 90, 77]
number0[0] = 98;

// for loop check 

for (let i = 0; i < number.length; i ++) {
    const numbers = number[i]
    console.log(numbers);
}

// function dicleare 

function multiple(num1, num2) {
    const result = num1 * num2;
    return result;
}

const output = multiple(77, 9);
console.log(output);

// object diclear
// accsess property object
const student = {
    name:  'Sakib Alhasan',
    age: 35,
    movis: ['king khan', 'dhakar mastan']
}

const myVariable = 'age';
console.log(student.age); // direct property access
console.log(student['age']); // access property by name string
console.log(student[myVariable]); // access property by name in a variavle

